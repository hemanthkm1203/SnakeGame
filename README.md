## Snake Game (Java Swing)

Simple Snake game built with plain Java/Swing.

### Requirements

- Java 17+ (or any version with Swing support)

### Run

```bash
javac -d out src/*.java
java -cp out SnakeGame
```

Use arrow keys to move. Press Enter to restart after game over.

